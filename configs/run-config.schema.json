{
  "$defs": {
    "AssemblyStep": {
      "$comment": "Steps this environment can name: ['drop', 'join', 'select', 'union']",
      "anyOf": [
        {
          "$comment": "These steps have required parameters and must use the object form: ['drop', 'join', 'select', 'union']",
          "description": "A step without parameters. A bare function name (`cap_single_name`), resolved in the template module for this kind of step or among the steps installed packages publish, or a qualified `package.module:function` importable by the engine and by any worker process.",
          "not": {
            "enum": [
              "drop",
              "join",
              "select",
              "union"
            ]
          },
          "pattern": "^(?:[A-Za-z_][A-Za-z0-9_]*(?:\\.[A-Za-z_][A-Za-z0-9_]*)*:)?[A-Za-z_][A-Za-z0-9_]*$",
          "type": "string"
        },
        {
          "additionalProperties": false,
          "allOf": [
            {
              "if": {
                "properties": {
                  "name": {
                    "const": "drop"
                  }
                },
                "required": [
                  "name"
                ]
              },
              "then": {
                "properties": {
                  "params": {
                    "additionalProperties": false,
                    "description": "Parameters for :func:`drop`.",
                    "properties": {
                      "datasets": {
                        "description": "Datasets to discard; they are not carried into any portfolio's bundle.",
                        "items": {
                          "type": "string"
                        },
                        "minItems": 1,
                        "title": "Datasets",
                        "type": "array"
                      }
                    },
                    "required": [
                      "datasets"
                    ],
                    "type": "object"
                  }
                },
                "required": [
                  "params"
                ]
              }
            },
            {
              "if": {
                "properties": {
                  "name": {
                    "const": "join"
                  }
                },
                "required": [
                  "name"
                ]
              },
              "then": {
                "properties": {
                  "params": {
                    "additionalProperties": false,
                    "description": "Parameters for :func:`join`.",
                    "properties": {
                      "cardinality": {
                        "$ref": "#/$defs/JoinCardinality",
                        "description": "Expected key cardinality, enforced by pandas `merge(validate=...)`: a duplicate key on the wrong side aborts the run instead of multiplying rows."
                      },
                      "columns": {
                        "anyOf": [
                          {
                            "items": {
                              "type": "string"
                            },
                            "type": "array"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "default": null,
                        "description": "Source columns to bring across, besides the keys. Default: every non-key column of `source`.",
                        "title": "Columns"
                      },
                      "how": {
                        "$ref": "#/$defs/JoinHow",
                        "default": "left",
                        "description": "`left` keeps every row of `into`; `inner` keeps only matched rows."
                      },
                      "into": {
                        "description": "The dataset that receives the columns.",
                        "minLength": 1,
                        "title": "Into",
                        "type": "string"
                      },
                      "on": {
                        "description": "Join key columns present in both datasets. Their dtypes are aligned to `into` before merging so a text key never silently becomes `object`.",
                        "items": {
                          "type": "string"
                        },
                        "minItems": 1,
                        "title": "On",
                        "type": "array"
                      },
                      "overwrite": {
                        "default": false,
                        "description": "Allow brought columns to replace columns `into` already has. Default: such a join is refused.",
                        "title": "Overwrite",
                        "type": "boolean"
                      },
                      "rename": {
                        "additionalProperties": {
                          "type": "string"
                        },
                        "description": "Source column to the name it takes in `into`, applied after `columns` is chosen.",
                        "title": "Rename",
                        "type": "object"
                      },
                      "require_all_matched": {
                        "default": false,
                        "description": "When true, every row of `into` must find a match in `source`; unmatched keys are reported and the run is rejected.",
                        "title": "Require All Matched",
                        "type": "boolean"
                      },
                      "source": {
                        "description": "The dataset the columns come from; any dataset other than `into`.",
                        "minLength": 1,
                        "title": "Source",
                        "type": "string"
                      }
                    },
                    "required": [
                      "into",
                      "source",
                      "on",
                      "cardinality"
                    ],
                    "type": "object"
                  }
                },
                "required": [
                  "params"
                ]
              }
            },
            {
              "if": {
                "properties": {
                  "name": {
                    "const": "select"
                  }
                },
                "required": [
                  "name"
                ]
              },
              "then": {
                "properties": {
                  "params": {
                    "additionalProperties": false,
                    "description": "Parameters for :func:`select`.",
                    "properties": {
                      "columns": {
                        "anyOf": [
                          {
                            "items": {
                              "type": "string"
                            },
                            "type": "array"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "default": null,
                        "description": "Keep exactly these columns, in this order. Mutually exclusive with `drop`.",
                        "title": "Columns"
                      },
                      "dataset": {
                        "description": "The dataset to trim.",
                        "minLength": 1,
                        "title": "Dataset",
                        "type": "string"
                      },
                      "drop": {
                        "default": [],
                        "description": "Columns to remove. Mutually exclusive with `columns`.",
                        "items": {
                          "type": "string"
                        },
                        "title": "Drop",
                        "type": "array"
                      },
                      "rename": {
                        "additionalProperties": {
                          "type": "string"
                        },
                        "description": "Old column name to new, applied after `columns` or `drop`.",
                        "title": "Rename",
                        "type": "object"
                      }
                    },
                    "required": [
                      "dataset"
                    ],
                    "type": "object"
                  }
                },
                "required": [
                  "params"
                ]
              }
            },
            {
              "if": {
                "properties": {
                  "name": {
                    "const": "union"
                  }
                },
                "required": [
                  "name"
                ]
              },
              "then": {
                "properties": {
                  "params": {
                    "additionalProperties": false,
                    "description": "Parameters for :func:`union`.",
                    "properties": {
                      "into": {
                        "description": "Name of the stacked result. An existing dataset of this name must be one of `sources`.",
                        "minLength": 1,
                        "title": "Into",
                        "type": "string"
                      },
                      "keep_sources": {
                        "default": false,
                        "description": "Keep the source datasets after stacking. Default: they are dropped, so only `into` is carried forward.",
                        "title": "Keep Sources",
                        "type": "boolean"
                      },
                      "source_column": {
                        "anyOf": [
                          {
                            "type": "string"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "default": null,
                        "description": "Optional column recording which source each row came from.",
                        "title": "Source Column"
                      },
                      "sources": {
                        "description": "Datasets stacked in order. Columns they share must agree on dtype; a column some lack is null there.",
                        "items": {
                          "type": "string"
                        },
                        "minItems": 1,
                        "title": "Sources",
                        "type": "array"
                      }
                    },
                    "required": [
                      "into",
                      "sources"
                    ],
                    "type": "object"
                  }
                },
                "required": [
                  "params"
                ]
              }
            }
          ],
          "description": "A step with parameters.",
          "properties": {
            "name": {
              "description": "A bare function name (`cap_single_name`), resolved in the template module for this kind of step or among the steps installed packages publish, or a qualified `package.module:function` importable by the engine and by any worker process.",
              "pattern": "^(?:[A-Za-z_][A-Za-z0-9_]*(?:\\.[A-Za-z_][A-Za-z0-9_]*)*:)?[A-Za-z_][A-Za-z0-9_]*$",
              "type": "string"
            },
            "params": {
              "description": "Parameters validated against the function's `params` model; for the steps this environment can name the exact shape is given below.",
              "type": "object"
            }
          },
          "required": [
            "name"
          ],
          "type": "object"
        }
      ],
      "description": "An assembly step from `assembly.py`: `(frames: Frames[, params]) -> Frames`, run once over every loaded dataset.",
      "title": "AssemblyStep"
    },
    "BuildStep": {
      "$comment": "Steps this environment can name: ['standard']",
      "anyOf": [
        {
          "description": "A step without parameters. A bare function name (`cap_single_name`), resolved in the template module for this kind of step or among the steps installed packages publish, or a qualified `package.module:function` importable by the engine and by any worker process.",
          "pattern": "^(?:[A-Za-z_][A-Za-z0-9_]*(?:\\.[A-Za-z_][A-Za-z0-9_]*)*:)?[A-Za-z_][A-Za-z0-9_]*$",
          "type": "string"
        },
        {
          "additionalProperties": false,
          "allOf": [
            {
              "if": {
                "properties": {
                  "name": {
                    "const": "standard"
                  }
                },
                "required": [
                  "name"
                ]
              },
              "then": {
                "properties": {
                  "params": {
                    "additionalProperties": false,
                    "description": "What the standard build lets a config decide: the start policy for the box.",
                    "properties": {
                      "hold_breached_starts": {
                        "default": false,
                        "description": "A name already held past a bound — over its cap, under its floor — is held where it is instead of failing the portfolio as a start the order flow cannot trade out of: its bound loosens to the current weight, and the box, the buyable set, the order clamp, and the verifier all read the loosened bound. Off, such a start is infeasible and named. A retry of an inflow or an outflow under its own config turns this on.",
                        "title": "Hold Breached Starts",
                        "type": "boolean"
                      }
                    },
                    "type": "object"
                  }
                }
              }
            }
          ],
          "description": "A step with parameters.",
          "properties": {
            "name": {
              "description": "A bare function name (`cap_single_name`), resolved in the template module for this kind of step or among the steps installed packages publish, or a qualified `package.module:function` importable by the engine and by any worker process.",
              "pattern": "^(?:[A-Za-z_][A-Za-z0-9_]*(?:\\.[A-Za-z_][A-Za-z0-9_]*)*:)?[A-Za-z_][A-Za-z0-9_]*$",
              "type": "string"
            },
            "params": {
              "description": "Parameters validated against the function's `params` model; for the steps this environment can name the exact shape is given below.",
              "type": "object"
            }
          },
          "required": [
            "name"
          ],
          "type": "object"
        }
      ],
      "description": "The build step from `engine/build.py`: `(data: PortfolioData[, params]) -> ProblemSpec`; `standard` is the default.",
      "title": "BuildStep"
    },
    "DatasetConfig": {
      "additionalProperties": false,
      "description": "How one input is loaded, what it depends on, and how its portfolios are partitioned across calls.",
      "properties": {
        "batch_size": {
          "anyOf": [
            {
              "minimum": 1,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "How many portfolios one call of a `per_portfolio` loader is given, as `request.portfolio_ids`. `1` is a call per portfolio; a larger number batches a source that takes an id list; omitted, every portfolio goes in one call. Ignored for a `global` dataset, which is loaded by one call.",
          "title": "Batch Size"
        },
        "depends_on": {
          "default": [],
          "description": "Names of other `datasets` entries this one needs. The engine starts every dataset the moment its dependencies have loaded and hands their frames to the loader as `request.inputs`. Declare `portfolios` to receive the book's ids as `request.portfolio_ids`; a `per_portfolio` dataset depends on `portfolios` implicitly. Default: no dependencies, so the loader starts the moment the load stage does.",
          "items": {
            "type": "string"
          },
          "title": "Depends On",
          "type": "array"
        },
        "loader": {
          "$ref": "#/$defs/LoaderStep",
          "description": "The loader step: `(request: LoadRequest[, params]) -> DataFrame`, plain or `async def`."
        },
        "max_in_flight": {
          "anyOf": [
            {
              "minimum": 1,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "How many of this dataset's calls the engine keeps in flight at once. A slot is held for the whole call, so `batch_size: 1` with `max_in_flight: 8` is eight concurrent per-portfolio calls to the source. Omit for no bound.",
          "title": "Max In Flight"
        },
        "scope": {
          "$ref": "#/$defs/DatasetScope",
          "default": "global",
          "description": "`global` (default): one call, and the dataset is visible to every assembly step. `per_portfolio`: the engine partitions the portfolio ids into batches and calls the loader once per batch, so a source that answers per account is driven by the engine rather than by the loader; a batch that fails, or that comes back without a portfolio's rows, fails those portfolios alone at stage `load` and the run carries on. A per-portfolio dataset is not passed to assembly — attach its columns in a rule instead."
        }
      },
      "required": [
        "loader"
      ],
      "title": "DatasetConfig",
      "type": "object"
    },
    "DatasetScope": {
      "description": "How a dataset is partitioned across loader calls; see `datasets.<name>.scope`.",
      "enum": [
        "global",
        "per_portfolio"
      ],
      "type": "string"
    },
    "DatasetSpec": {
      "anyOf": [
        {
          "$ref": "#/$defs/DatasetConfig"
        },
        {
          "$ref": "#/$defs/InlinePortfolios"
        }
      ],
      "description": "One entry of `datasets`: a loaded input, or — for `portfolios` only — the inline list of ids."
    },
    "Dependencies": {
      "description": "Which higher-priority portfolios a portfolio waits for; see `execution.dependencies`.",
      "enum": [
        "overlap",
        "all"
      ],
      "type": "string"
    },
    "ExecutionConfig": {
      "additionalProperties": false,
      "description": "What a failure means, and which portfolios wait for which.\n\nThe schedule itself is derived: every portfolio builds in parallel, then solves once the\nhigher-priority portfolios it depends on have solved. Which cluster the run provisions for itself\nand how many workers it has are settings (`PORTFOLIO_OPTIMIZER_CLUSTER`,\n`PORTFOLIO_OPTIMIZER_MAX_WORKERS`, ...), not config, so the same config hashes the same on a laptop\nand on a cluster — and nothing here changes a portfolio's answer, only how long the run takes.",
      "properties": {
        "dependencies": {
          "$ref": "#/$defs/Dependencies",
          "default": "overlap",
          "description": "Which higher-priority portfolios a portfolio waits for. `overlap` (default): those that can trade a security it can trade too, on the side the run couples through (buys under `inflow`, sells under `outflow`, either under `rebalance`) — and only what its own constraints *declare* they read: a typed constraint row says whether it reads the chain and, through its `scope`, which securities it couples through, so a portfolio whose rows read no chain waits for nobody. A chain-aware objective term or a solve step other than the shipped `cvxpy` one couples conservatively through the whole tradable set; with nothing anywhere reading the chain, nothing waits. `all`: every higher-priority portfolio is a predecessor, one line — the same answer, for diagnosis."
        },
        "on_error": {
          "$ref": "#/$defs/OnError",
          "default": "fail_fast",
          "description": "`fail_fast` stops at the first failed portfolio: every lower-priority portfolio is recorded as skipped. `continue` isolates the failure: only the portfolios that depended on it are skipped."
        }
      },
      "title": "ExecutionConfig",
      "type": "object"
    },
    "InlinePortfolios": {
      "additionalProperties": false,
      "description": "The portfolio list written directly in the config instead of loaded: `{\"ids\": [\"P7\", \"P2\"]}`, or the bare array as shorthand.\n\nThe written order is the solve order — the first id solves first. Only the `portfolios` dataset may\nbe written inline; it costs nothing to load, so every dataset that depends on it starts at once.\nA book too large to write out, or one whose priorities come from data, uses a loader instead.",
      "properties": {
        "ids": {
          "description": "The portfolio ids, in solve order: the first id solves first, and the engine records `solve_order` as the position.",
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "title": "Ids",
          "type": "array"
        }
      },
      "required": [
        "ids"
      ],
      "title": "InlinePortfolios",
      "type": "object"
    },
    "JoinCardinality": {
      "description": "Expected key cardinality of a join, enforced by pandas.",
      "enum": [
        "one_to_one",
        "one_to_many",
        "many_to_one"
      ],
      "type": "string"
    },
    "JoinHow": {
      "description": "Join type: keep every left row, or only matched rows.",
      "enum": [
        "left",
        "inner"
      ],
      "type": "string"
    },
    "LoaderStep": {
      "$comment": "Steps this environment can name: ['load_constraints', 'load_details', 'load_holdings', 'load_mandates', 'load_parameters', 'load_portfolios', 'load_trades', 'load_universe']",
      "anyOf": [
        {
          "description": "A step without parameters. A bare function name (`cap_single_name`), resolved in the template module for this kind of step or among the steps installed packages publish, or a qualified `package.module:function` importable by the engine and by any worker process.",
          "pattern": "^(?:[A-Za-z_][A-Za-z0-9_]*(?:\\.[A-Za-z_][A-Za-z0-9_]*)*:)?[A-Za-z_][A-Za-z0-9_]*$",
          "type": "string"
        },
        {
          "additionalProperties": false,
          "allOf": [
            {
              "if": {
                "properties": {
                  "name": {
                    "const": "load_constraints"
                  }
                },
                "required": [
                  "name"
                ]
              },
              "then": {
                "properties": {
                  "params": {
                    "additionalProperties": false,
                    "description": "What every shipped loader accepts: how long its source is pretended to take.\n\nEach loader draws a wait from its own band — :data:`SECURITY_MASTER` is tens of seconds, a\nparameter store is under one — seeded on the run id, the dataset, and the ids in the call, so two\nruns of one config wait the same amounts. Setting both fields overrides that band; setting both to\nzero removes the wait, which is what a test does. A real loader has none of this: delete the field\nand the wait, and keep the rest.",
                    "properties": {
                      "max_latency_s": {
                        "anyOf": [
                          {
                            "minimum": 0,
                            "type": "number"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "default": null,
                        "description": "Longest this source may take to answer, in seconds. Omit for the loader's own band.",
                        "title": "Max Latency S"
                      },
                      "min_latency_s": {
                        "anyOf": [
                          {
                            "minimum": 0,
                            "type": "number"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "default": null,
                        "description": "Shortest this source may take to answer, in seconds. Omit for the loader's own band.",
                        "title": "Min Latency S"
                      }
                    },
                    "type": "object"
                  }
                }
              }
            },
            {
              "if": {
                "properties": {
                  "name": {
                    "const": "load_details"
                  }
                },
                "required": [
                  "name"
                ]
              },
              "then": {
                "properties": {
                  "params": {
                    "additionalProperties": false,
                    "description": "What every shipped loader accepts: how long its source is pretended to take.\n\nEach loader draws a wait from its own band — :data:`SECURITY_MASTER` is tens of seconds, a\nparameter store is under one — seeded on the run id, the dataset, and the ids in the call, so two\nruns of one config wait the same amounts. Setting both fields overrides that band; setting both to\nzero removes the wait, which is what a test does. A real loader has none of this: delete the field\nand the wait, and keep the rest.",
                    "properties": {
                      "max_latency_s": {
                        "anyOf": [
                          {
                            "minimum": 0,
                            "type": "number"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "default": null,
                        "description": "Longest this source may take to answer, in seconds. Omit for the loader's own band.",
                        "title": "Max Latency S"
                      },
                      "min_latency_s": {
                        "anyOf": [
                          {
                            "minimum": 0,
                            "type": "number"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "default": null,
                        "description": "Shortest this source may take to answer, in seconds. Omit for the loader's own band.",
                        "title": "Min Latency S"
                      }
                    },
                    "type": "object"
                  }
                }
              }
            },
            {
              "if": {
                "properties": {
                  "name": {
                    "const": "load_holdings"
                  }
                },
                "required": [
                  "name"
                ]
              },
              "then": {
                "properties": {
                  "params": {
                    "additionalProperties": false,
                    "description": "What every shipped loader accepts: how long its source is pretended to take.\n\nEach loader draws a wait from its own band — :data:`SECURITY_MASTER` is tens of seconds, a\nparameter store is under one — seeded on the run id, the dataset, and the ids in the call, so two\nruns of one config wait the same amounts. Setting both fields overrides that band; setting both to\nzero removes the wait, which is what a test does. A real loader has none of this: delete the field\nand the wait, and keep the rest.",
                    "properties": {
                      "max_latency_s": {
                        "anyOf": [
                          {
                            "minimum": 0,
                            "type": "number"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "default": null,
                        "description": "Longest this source may take to answer, in seconds. Omit for the loader's own band.",
                        "title": "Max Latency S"
                      },
                      "min_latency_s": {
                        "anyOf": [
                          {
                            "minimum": 0,
                            "type": "number"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "default": null,
                        "description": "Shortest this source may take to answer, in seconds. Omit for the loader's own band.",
                        "title": "Min Latency S"
                      }
                    },
                    "type": "object"
                  }
                }
              }
            },
            {
              "if": {
                "properties": {
                  "name": {
                    "const": "load_mandates"
                  }
                },
                "required": [
                  "name"
                ]
              },
              "then": {
                "properties": {
                  "params": {
                    "additionalProperties": false,
                    "description": "What every shipped loader accepts: how long its source is pretended to take.\n\nEach loader draws a wait from its own band — :data:`SECURITY_MASTER` is tens of seconds, a\nparameter store is under one — seeded on the run id, the dataset, and the ids in the call, so two\nruns of one config wait the same amounts. Setting both fields overrides that band; setting both to\nzero removes the wait, which is what a test does. A real loader has none of this: delete the field\nand the wait, and keep the rest.",
                    "properties": {
                      "max_latency_s": {
                        "anyOf": [
                          {
                            "minimum": 0,
                            "type": "number"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "default": null,
                        "description": "Longest this source may take to answer, in seconds. Omit for the loader's own band.",
                        "title": "Max Latency S"
                      },
                      "min_latency_s": {
                        "anyOf": [
                          {
                            "minimum": 0,
                            "type": "number"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "default": null,
                        "description": "Shortest this source may take to answer, in seconds. Omit for the loader's own band.",
                        "title": "Min Latency S"
                      }
                    },
                    "type": "object"
                  }
                }
              }
            },
            {
              "if": {
                "properties": {
                  "name": {
                    "const": "load_parameters"
                  }
                },
                "required": [
                  "name"
                ]
              },
              "then": {
                "properties": {
                  "params": {
                    "additionalProperties": false,
                    "description": "Parameters for :func:`load_parameters`.",
                    "properties": {
                      "max_latency_s": {
                        "anyOf": [
                          {
                            "minimum": 0,
                            "type": "number"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "default": null,
                        "description": "Longest this source may take to answer, in seconds. Omit for the loader's own band.",
                        "title": "Max Latency S"
                      },
                      "min_latency_s": {
                        "anyOf": [
                          {
                            "minimum": 0,
                            "type": "number"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "default": null,
                        "description": "Shortest this source may take to answer, in seconds. Omit for the loader's own band.",
                        "title": "Min Latency S"
                      },
                      "set_name": {
                        "anyOf": [
                          {
                            "minLength": 1,
                            "type": "string"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "default": null,
                        "description": "Which parameter set to fetch. Omit to fetch the one named by the dataset itself, which is the usual arrangement.",
                        "title": "Set Name"
                      }
                    },
                    "type": "object"
                  }
                }
              }
            },
            {
              "if": {
                "properties": {
                  "name": {
                    "const": "load_portfolios"
                  }
                },
                "required": [
                  "name"
                ]
              },
              "then": {
                "properties": {
                  "params": {
                    "additionalProperties": false,
                    "description": "What every shipped loader accepts: how long its source is pretended to take.\n\nEach loader draws a wait from its own band — :data:`SECURITY_MASTER` is tens of seconds, a\nparameter store is under one — seeded on the run id, the dataset, and the ids in the call, so two\nruns of one config wait the same amounts. Setting both fields overrides that band; setting both to\nzero removes the wait, which is what a test does. A real loader has none of this: delete the field\nand the wait, and keep the rest.",
                    "properties": {
                      "max_latency_s": {
                        "anyOf": [
                          {
                            "minimum": 0,
                            "type": "number"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "default": null,
                        "description": "Longest this source may take to answer, in seconds. Omit for the loader's own band.",
                        "title": "Max Latency S"
                      },
                      "min_latency_s": {
                        "anyOf": [
                          {
                            "minimum": 0,
                            "type": "number"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "default": null,
                        "description": "Shortest this source may take to answer, in seconds. Omit for the loader's own band.",
                        "title": "Min Latency S"
                      }
                    },
                    "type": "object"
                  }
                }
              }
            },
            {
              "if": {
                "properties": {
                  "name": {
                    "const": "load_trades"
                  }
                },
                "required": [
                  "name"
                ]
              },
              "then": {
                "properties": {
                  "params": {
                    "additionalProperties": false,
                    "description": "What every shipped loader accepts: how long its source is pretended to take.\n\nEach loader draws a wait from its own band — :data:`SECURITY_MASTER` is tens of seconds, a\nparameter store is under one — seeded on the run id, the dataset, and the ids in the call, so two\nruns of one config wait the same amounts. Setting both fields overrides that band; setting both to\nzero removes the wait, which is what a test does. A real loader has none of this: delete the field\nand the wait, and keep the rest.",
                    "properties": {
                      "max_latency_s": {
                        "anyOf": [
                          {
                            "minimum": 0,
                            "type": "number"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "default": null,
                        "description": "Longest this source may take to answer, in seconds. Omit for the loader's own band.",
                        "title": "Max Latency S"
                      },
                      "min_latency_s": {
                        "anyOf": [
                          {
                            "minimum": 0,
                            "type": "number"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "default": null,
                        "description": "Shortest this source may take to answer, in seconds. Omit for the loader's own band.",
                        "title": "Min Latency S"
                      }
                    },
                    "type": "object"
                  }
                }
              }
            },
            {
              "if": {
                "properties": {
                  "name": {
                    "const": "load_universe"
                  }
                },
                "required": [
                  "name"
                ]
              },
              "then": {
                "properties": {
                  "params": {
                    "additionalProperties": false,
                    "description": "What every shipped loader accepts: how long its source is pretended to take.\n\nEach loader draws a wait from its own band — :data:`SECURITY_MASTER` is tens of seconds, a\nparameter store is under one — seeded on the run id, the dataset, and the ids in the call, so two\nruns of one config wait the same amounts. Setting both fields overrides that band; setting both to\nzero removes the wait, which is what a test does. A real loader has none of this: delete the field\nand the wait, and keep the rest.",
                    "properties": {
                      "max_latency_s": {
                        "anyOf": [
                          {
                            "minimum": 0,
                            "type": "number"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "default": null,
                        "description": "Longest this source may take to answer, in seconds. Omit for the loader's own band.",
                        "title": "Max Latency S"
                      },
                      "min_latency_s": {
                        "anyOf": [
                          {
                            "minimum": 0,
                            "type": "number"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "default": null,
                        "description": "Shortest this source may take to answer, in seconds. Omit for the loader's own band.",
                        "title": "Min Latency S"
                      }
                    },
                    "type": "object"
                  }
                }
              }
            }
          ],
          "description": "A step with parameters.",
          "properties": {
            "name": {
              "description": "A bare function name (`cap_single_name`), resolved in the template module for this kind of step or among the steps installed packages publish, or a qualified `package.module:function` importable by the engine and by any worker process.",
              "pattern": "^(?:[A-Za-z_][A-Za-z0-9_]*(?:\\.[A-Za-z_][A-Za-z0-9_]*)*:)?[A-Za-z_][A-Za-z0-9_]*$",
              "type": "string"
            },
            "params": {
              "description": "Parameters validated against the function's `params` model; for the steps this environment can name the exact shape is given below.",
              "type": "object"
            }
          },
          "required": [
            "name"
          ],
          "type": "object"
        }
      ],
      "description": "A dataset loader from `loaders.py`: `(request: LoadRequest[, params]) -> DataFrame`, plain or `async def`.",
      "title": "LoaderStep"
    },
    "OnError": {
      "description": "What happens after a portfolio fails.",
      "enum": [
        "fail_fast",
        "continue"
      ],
      "type": "string"
    },
    "OrderFlow": {
      "description": "The run's order flow: `inflow` (the run buys), `outflow` (the run sells), or `rebalance` (either way, no cash moved on purpose); see `order_flow`.",
      "enum": [
        "inflow",
        "outflow",
        "rebalance"
      ],
      "type": "string"
    },
    "PostSolveConfig": {
      "additionalProperties": false,
      "description": "Tolerances for the independent, cvxpy-free verification of every solution.",
      "properties": {
        "objective_abs_tol": {
          "default": 1e-09,
          "description": "Absolute tolerance between the recomputed and the solver-reported objective.",
          "exclusiveMinimum": 0,
          "title": "Objective Abs Tol",
          "type": "number"
        },
        "objective_rel_tol": {
          "default": 1e-05,
          "description": "Relative tolerance between the recomputed and the solver-reported objective.",
          "exclusiveMinimum": 0,
          "title": "Objective Rel Tol",
          "type": "number"
        },
        "violation_tol": {
          "default": 1e-06,
          "description": "Maximum constraint violation accepted by the verifier; deliberately looser than the solver's own tolerance.",
          "exclusiveMinimum": 0,
          "title": "Violation Tol",
          "type": "number"
        }
      },
      "title": "PostSolveConfig",
      "type": "object"
    },
    "RuleStep": {
      "$comment": "Steps this environment can name: ['add_zero_alpha', 'attach_universe_columns', 'cap_single_name', 'restrict_low_liquidity', 'restrict_recent_trades', 'restrict_to_mandate']",
      "anyOf": [
        {
          "$comment": "These steps have required parameters and must use the object form: ['cap_single_name']",
          "description": "A step without parameters. A bare function name (`cap_single_name`), resolved in the template module for this kind of step or among the steps installed packages publish, or a qualified `package.module:function` importable by the engine and by any worker process.",
          "not": {
            "enum": [
              "cap_single_name"
            ]
          },
          "pattern": "^(?:[A-Za-z_][A-Za-z0-9_]*(?:\\.[A-Za-z_][A-Za-z0-9_]*)*:)?[A-Za-z_][A-Za-z0-9_]*$",
          "type": "string"
        },
        {
          "additionalProperties": false,
          "allOf": [
            {
              "if": {
                "properties": {
                  "name": {
                    "const": "add_zero_alpha"
                  }
                },
                "required": [
                  "name"
                ]
              },
              "then": {
                "properties": {
                  "params": {
                    "additionalProperties": false,
                    "description": "`add_zero_alpha` takes no parameters.",
                    "maxProperties": 0,
                    "type": "object"
                  }
                }
              }
            },
            {
              "if": {
                "properties": {
                  "name": {
                    "const": "attach_universe_columns"
                  }
                },
                "required": [
                  "name"
                ]
              },
              "then": {
                "properties": {
                  "params": {
                    "additionalProperties": false,
                    "description": "Parameters for :func:`attach_universe_columns`.",
                    "properties": {
                      "columns": {
                        "default": [],
                        "description": "Universe columns to copy onto holdings, matched on `security_id`. Default: every column the universe carries beyond its schema — the analytics columns.",
                        "items": {
                          "type": "string"
                        },
                        "title": "Columns",
                        "type": "array"
                      }
                    },
                    "type": "object"
                  }
                }
              }
            },
            {
              "if": {
                "properties": {
                  "name": {
                    "const": "cap_single_name"
                  }
                },
                "required": [
                  "name"
                ]
              },
              "then": {
                "properties": {
                  "params": {
                    "additionalProperties": false,
                    "description": "Parameters for :func:`cap_single_name`.",
                    "properties": {
                      "max_weight": {
                        "anyOf": [
                          {
                            "exclusiveMinimum": 0.0,
                            "maximum": 1.0,
                            "type": "number"
                          },
                          {
                            "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$",
                            "type": "string"
                          }
                        ],
                        "title": "Max Weight"
                      }
                    },
                    "required": [
                      "max_weight"
                    ],
                    "type": "object"
                  }
                },
                "required": [
                  "params"
                ]
              }
            },
            {
              "if": {
                "properties": {
                  "name": {
                    "const": "restrict_low_liquidity"
                  }
                },
                "required": [
                  "name"
                ]
              },
              "then": {
                "properties": {
                  "params": {
                    "additionalProperties": false,
                    "description": "Where :func:`restrict_low_liquidity` reads its threshold: an extra dataset of named values, not a number in the config.",
                    "properties": {
                      "dataset": {
                        "default": "buy_universe_parameters",
                        "minLength": 1,
                        "title": "Dataset",
                        "type": "string"
                      },
                      "key": {
                        "default": "min_adv_shares",
                        "minLength": 1,
                        "title": "Key",
                        "type": "string"
                      }
                    },
                    "type": "object"
                  }
                }
              }
            },
            {
              "if": {
                "properties": {
                  "name": {
                    "const": "restrict_recent_trades"
                  }
                },
                "required": [
                  "name"
                ]
              },
              "then": {
                "properties": {
                  "params": {
                    "additionalProperties": false,
                    "description": "Where :func:`restrict_recent_trades` reads the account's recent trades, and how far back a trade still counts.",
                    "properties": {
                      "dataset": {
                        "default": "trades",
                        "minLength": 1,
                        "title": "Dataset",
                        "type": "string"
                      },
                      "window_days": {
                        "default": 30,
                        "description": "A name traded within this many days of the run's as-of instant is frozen; thirty is the US wash-sale window.",
                        "minimum": 0,
                        "title": "Window Days",
                        "type": "integer"
                      }
                    },
                    "type": "object"
                  }
                }
              }
            },
            {
              "if": {
                "properties": {
                  "name": {
                    "const": "restrict_to_mandate"
                  }
                },
                "required": [
                  "name"
                ]
              },
              "then": {
                "properties": {
                  "params": {
                    "additionalProperties": false,
                    "description": "Where :func:`restrict_to_mandate` reads the account's mandate: an extra dataset of ``portfolio_id``/``sector`` rows.",
                    "properties": {
                      "dataset": {
                        "default": "mandates",
                        "minLength": 1,
                        "title": "Dataset",
                        "type": "string"
                      }
                    },
                    "type": "object"
                  }
                }
              }
            }
          ],
          "description": "A step with parameters.",
          "properties": {
            "name": {
              "description": "A bare function name (`cap_single_name`), resolved in the template module for this kind of step or among the steps installed packages publish, or a qualified `package.module:function` importable by the engine and by any worker process.",
              "pattern": "^(?:[A-Za-z_][A-Za-z0-9_]*(?:\\.[A-Za-z_][A-Za-z0-9_]*)*:)?[A-Za-z_][A-Za-z0-9_]*$",
              "type": "string"
            },
            "params": {
              "description": "Parameters validated against the function's `params` model; for the steps this environment can name the exact shape is given below.",
              "type": "object"
            }
          },
          "required": [
            "name"
          ],
          "type": "object"
        }
      ],
      "description": "A business-logic rule from `rules.py`: `(data: PortfolioData[, params]) -> PortfolioData`.",
      "title": "RuleStep"
    },
    "RunMeta": {
      "additionalProperties": false,
      "description": "Identity of the run, recorded in the manifest and kept out of the config hash.",
      "properties": {
        "name": {
          "description": "Human-readable run name, e.g. `daily_rebalance`.",
          "minLength": 1,
          "title": "Name",
          "type": "string"
        },
        "tags": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Free-form string labels copied into the manifest, e.g. `{\"desk\": \"tax-aware\"}`.",
          "title": "Tags",
          "type": "object"
        }
      },
      "required": [
        "name"
      ],
      "title": "RunMeta",
      "type": "object"
    },
    "SinkStep": {
      "$comment": "Steps this environment can name: ['orders_to_csv', 'orders_to_parquet']",
      "anyOf": [
        {
          "description": "A step without parameters. A bare function name (`cap_single_name`), resolved in the template module for this kind of step or among the steps installed packages publish, or a qualified `package.module:function` importable by the engine and by any worker process.",
          "pattern": "^(?:[A-Za-z_][A-Za-z0-9_]*(?:\\.[A-Za-z_][A-Za-z0-9_]*)*:)?[A-Za-z_][A-Za-z0-9_]*$",
          "type": "string"
        },
        {
          "additionalProperties": false,
          "allOf": [
            {
              "if": {
                "properties": {
                  "name": {
                    "const": "orders_to_csv"
                  }
                },
                "required": [
                  "name"
                ]
              },
              "then": {
                "properties": {
                  "params": {
                    "additionalProperties": false,
                    "description": "Where under the run's output directory to write.",
                    "properties": {
                      "subdir": {
                        "default": "orders",
                        "minLength": 1,
                        "title": "Subdir",
                        "type": "string"
                      }
                    },
                    "type": "object"
                  }
                }
              }
            },
            {
              "if": {
                "properties": {
                  "name": {
                    "const": "orders_to_parquet"
                  }
                },
                "required": [
                  "name"
                ]
              },
              "then": {
                "properties": {
                  "params": {
                    "additionalProperties": false,
                    "description": "Where under the run's output directory to write.",
                    "properties": {
                      "subdir": {
                        "default": "orders",
                        "minLength": 1,
                        "title": "Subdir",
                        "type": "string"
                      }
                    },
                    "type": "object"
                  }
                }
              }
            }
          ],
          "description": "A step with parameters.",
          "properties": {
            "name": {
              "description": "A bare function name (`cap_single_name`), resolved in the template module for this kind of step or among the steps installed packages publish, or a qualified `package.module:function` importable by the engine and by any worker process.",
              "pattern": "^(?:[A-Za-z_][A-Za-z0-9_]*(?:\\.[A-Za-z_][A-Za-z0-9_]*)*:)?[A-Za-z_][A-Za-z0-9_]*$",
              "type": "string"
            },
            "params": {
              "description": "Parameters validated against the function's `params` model; for the steps this environment can name the exact shape is given below.",
              "type": "object"
            }
          },
          "required": [
            "name"
          ],
          "type": "object"
        }
      ],
      "description": "An order sink from `sinks.py`: `(orders: DataFrame, io: IoContext[, params]) -> tuple[Artifact, ...]`.",
      "title": "SinkStep"
    },
    "SolveOrderStep": {
      "$comment": "Steps this environment can name: ['most_uninvested_first']",
      "anyOf": [
        {
          "description": "A step without parameters. A bare function name (`cap_single_name`), resolved in the template module for this kind of step or among the steps installed packages publish, or a qualified `package.module:function` importable by the engine and by any worker process.",
          "pattern": "^(?:[A-Za-z_][A-Za-z0-9_]*(?:\\.[A-Za-z_][A-Za-z0-9_]*)*:)?[A-Za-z_][A-Za-z0-9_]*$",
          "type": "string"
        },
        {
          "additionalProperties": false,
          "allOf": [
            {
              "if": {
                "properties": {
                  "name": {
                    "const": "most_uninvested_first"
                  }
                },
                "required": [
                  "name"
                ]
              },
              "then": {
                "properties": {
                  "params": {
                    "additionalProperties": false,
                    "description": "`most_uninvested_first` takes no parameters.",
                    "maxProperties": 0,
                    "type": "object"
                  }
                }
              }
            }
          ],
          "description": "A step with parameters.",
          "properties": {
            "name": {
              "description": "A bare function name (`cap_single_name`), resolved in the template module for this kind of step or among the steps installed packages publish, or a qualified `package.module:function` importable by the engine and by any worker process.",
              "pattern": "^(?:[A-Za-z_][A-Za-z0-9_]*(?:\\.[A-Za-z_][A-Za-z0-9_]*)*:)?[A-Za-z_][A-Za-z0-9_]*$",
              "type": "string"
            },
            "params": {
              "description": "Parameters validated against the function's `params` model; for the steps this environment can name the exact shape is given below.",
              "type": "object"
            }
          },
          "required": [
            "name"
          ],
          "type": "object"
        }
      ],
      "description": "A solve-order step from `solve_order.py`: `(data: PortfolioData[, params]) -> Decimal`; lower keys solve first, ties break on `portfolio_id`.",
      "title": "SolveOrderStep"
    },
    "SolveStep": {
      "$comment": "Steps this environment can name: ['cvxpy', 'pro_rata_fill']",
      "anyOf": [
        {
          "description": "A step without parameters. A bare function name (`cap_single_name`), resolved in the template module for this kind of step or among the steps installed packages publish, or a qualified `package.module:function` importable by the engine and by any worker process.",
          "pattern": "^(?:[A-Za-z_][A-Za-z0-9_]*(?:\\.[A-Za-z_][A-Za-z0-9_]*)*:)?[A-Za-z_][A-Za-z0-9_]*$",
          "type": "string"
        },
        {
          "additionalProperties": false,
          "allOf": [
            {
              "if": {
                "properties": {
                  "name": {
                    "const": "cvxpy"
                  }
                },
                "required": [
                  "name"
                ]
              },
              "then": {
                "properties": {
                  "params": {
                    "additionalProperties": false,
                    "description": "Which cvxpy solver runs and with what options.\n\n``solver`` must be one the adapter knows and cvxpy has installed — `CLARABEL`, `OSQP`, `SCS`,\n`HIGHS` (installed with cvxpy) or `PIQP` (the `piqp` extra) — checked when the config resolves,\nby `validate-config`, at the start of `run`, and on every worker before it does any work; there is\nno automatic fallback.",
                    "properties": {
                      "options": {
                        "additionalProperties": {
                          "anyOf": [
                            {
                              "type": "number"
                            },
                            {
                              "type": "integer"
                            },
                            {
                              "type": "boolean"
                            },
                            {
                              "type": "string"
                            }
                          ]
                        },
                        "description": "Passed verbatim to `Problem.solve(**options)`, e.g. `{\"max_iter\": 200}`.",
                        "title": "Options",
                        "type": "object"
                      },
                      "solver": {
                        "default": "CLARABEL",
                        "description": "The cvxpy solver: `CLARABEL` (default), `OSQP`, `SCS`, `HIGHS`, or `PIQP`.",
                        "minLength": 1,
                        "title": "Solver",
                        "type": "string"
                      },
                      "time_limit_s": {
                        "anyOf": [
                          {
                            "exclusiveMinimum": 0,
                            "type": "number"
                          },
                          {
                            "type": "null"
                          }
                        ],
                        "default": null,
                        "description": "Wall-clock limit per solve in seconds, translated to the solver's own option; omit for no limit. Rejected at resolve for a solver with no such option (`PIQP`).",
                        "title": "Time Limit S"
                      },
                      "verbose": {
                        "default": false,
                        "description": "Let the solver print its iteration log.",
                        "title": "Verbose",
                        "type": "boolean"
                      }
                    },
                    "type": "object"
                  }
                }
              }
            },
            {
              "if": {
                "properties": {
                  "name": {
                    "const": "pro_rata_fill"
                  }
                },
                "required": [
                  "name"
                ]
              },
              "then": {
                "properties": {
                  "params": {
                    "additionalProperties": false,
                    "description": "`pro_rata_fill` takes no parameters.",
                    "maxProperties": 0,
                    "type": "object"
                  }
                }
              }
            }
          ],
          "description": "A step with parameters.",
          "properties": {
            "name": {
              "description": "A bare function name (`cap_single_name`), resolved in the template module for this kind of step or among the steps installed packages publish, or a qualified `package.module:function` importable by the engine and by any worker process.",
              "pattern": "^(?:[A-Za-z_][A-Za-z0-9_]*(?:\\.[A-Za-z_][A-Za-z0-9_]*)*:)?[A-Za-z_][A-Za-z0-9_]*$",
              "type": "string"
            },
            "params": {
              "description": "Parameters validated against the function's `params` model; for the steps this environment can name the exact shape is given below.",
              "type": "object"
            }
          },
          "required": [
            "name"
          ],
          "type": "object"
        }
      ],
      "description": "The solve step from `solvers.py`: `(request: SolveRequest[, params]) -> SolveResult`; `cvxpy` is the default.",
      "title": "SolveStep"
    },
    "Vector": {
      "description": "The decision quantity a term or constraint reads: the target weight `w`, the `buy` or `sell` split, or `trade`, the amount traded on the side the run has.",
      "enum": [
        "w",
        "buy",
        "sell",
        "trade"
      ],
      "type": "string"
    }
  },
  "$id": "https://raw.githubusercontent.com/jacobragsdale/portfolio-optimizer/main/configs/run-config.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "A portfolio-optimizer run: what to load, how to combine it, which rules and terms apply, and how to execute.\n\nValidated strictly: unknown keys are errors, money is written as strings. Step names are\nresolved and their parameters validated — and every objective term is validated against its kind —\nbefore any data is loaded.",
  "else": {
    "$comment": "Without assembly steps, nothing can produce the engine-known frames, so every one must be loaded.",
    "properties": {
      "datasets": {
        "required": [
          "holdings",
          "universe",
          "details"
        ]
      }
    }
  },
  "if": {
    "properties": {
      "assembly": {
        "minItems": 1
      }
    },
    "required": [
      "assembly"
    ]
  },
  "properties": {
    "$schema": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Optional pointer to the JSON Schema for editor validation; ignored by the engine.",
      "title": "$Schema"
    },
    "assembly": {
      "default": [],
      "description": "Assembly steps from `assembly.py`, applied in order to the loaded datasets before the engine-known frames are validated: `join`, `union`, `select`, `drop`, or any custom `(frames: Frames[, params]) -> Frames` function. This is where analytics columns are attached to `holdings` and `universe`.",
      "items": {
        "$ref": "#/$defs/AssemblyStep"
      },
      "title": "Assembly",
      "type": "array"
    },
    "build": {
      "$ref": "#/$defs/BuildStep",
      "description": "The build step: `(data: PortfolioData[, params]) -> ProblemSpec`, run per portfolio after its rules. `standard` (default, in `engine/build.py`) aligns the bundle to the sorted universe, computes weights and tax per dollar exactly, derives the bounds, and exports every extra column, flag, grouping, and account scalar by name; a qualified name plugs in a build that reads the bundle its own way — tax lots, a factor block, a different bounds policy."
    },
    "datasets": {
      "$comment": "`portfolios` is always required; ['holdings', 'universe', 'details'] are required unless an assembly step produces them. `constraints` is engine-known but optional. Any other key is an extra dataset, available to assembly steps and carried into each portfolio's bundle.",
      "additionalProperties": {
        "$ref": "#/$defs/DatasetConfig"
      },
      "description": "Named datasets, every one a frame. `portfolios` is required — the list of portfolio ids and their `solve_order` priorities (lower solves first, ties break on `portfolio_id`), loaded like any dataset or written inline as a list of ids — and is consumed by the engine rather than passed to assembly. `holdings`, `universe`, and `details` must be declared unless an assembly step produces them; `constraints` is engine-known but optional, and a run that omits it constrains nothing beyond the trade identity and the spec's own bounds. Any other name is an extra dataset: available to every assembly step by name and carried into each portfolio's bundle as `data.extras` (reduced to the portfolio's rows when it has a `portfolio_id` column). Each dataset's loader starts the moment its `depends_on` dependencies have loaded — with none, the moment the load stage does.",
      "properties": {
        "constraints": {
          "$ref": "#/$defs/DatasetConfig"
        },
        "details": {
          "$ref": "#/$defs/DatasetConfig"
        },
        "holdings": {
          "$ref": "#/$defs/DatasetConfig"
        },
        "portfolios": {
          "anyOf": [
            {
              "allOf": [
                {
                  "$ref": "#/$defs/DatasetConfig"
                },
                {
                  "properties": {
                    "scope": {
                      "const": "global"
                    }
                  }
                }
              ]
            },
            {
              "$ref": "#/$defs/InlinePortfolios"
            },
            {
              "$comment": "A bare array is shorthand for {\"ids\": [...]}.",
              "items": {
                "minLength": 1,
                "type": "string"
              },
              "minItems": 1,
              "type": "array"
            }
          ],
          "description": "The portfolio list: a loaded dataset (always `scope: global`), or the ids written inline — `{\"ids\": [...]}` or the bare array, whose written order is the solve order."
        },
        "universe": {
          "$ref": "#/$defs/DatasetConfig"
        }
      },
      "required": [
        "portfolios"
      ],
      "title": "Datasets",
      "type": "object"
    },
    "execution": {
      "$ref": "#/$defs/ExecutionConfig",
      "description": "Failure semantics and how dependencies between portfolios are derived."
    },
    "objective": {
      "$comment": "Term kinds this environment can name: ['linear']",
      "default": [],
      "description": "Objective terms, each an object whose `kind` names a typed term model — the shipped `linear` (`{\"kind\": \"linear\", \"name\": \"alpha\", \"column\": \"alpha\", \"weight\": \"-1\"}`), or a kind an installed package publishes. The engine minimizes their sum; a reward is a negative weight. Every term is validated against its model when the config resolves, and its `name` must be unique. Empty is a run whose solve step minimizes nothing.",
      "items": {
        "anyOf": [
          {
            "additionalProperties": false,
            "description": "``weight · cᵀv``: a per-security column against a decision vector — ``alpha`` against ``w`` with a negative weight is the expected-return reward, ``tax_per_dollar`` against ``sell`` the tax cost, ``tcost_per_dollar`` against ``trade`` the trading cost.\n\nOmit ``column`` and every name counts once, so ``trade`` alone is a turnover penalty.",
            "properties": {
              "column": {
                "anyOf": [
                  {
                    "minLength": 1,
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "A per-security column of the spec — `alpha`, `tax_per_dollar`, `tcost_per_dollar`, any exported universe column; omitted, every name counts once.",
                "title": "Column"
              },
              "kind": {
                "const": "linear",
                "default": "linear",
                "description": "The kind: a per-security column against a decision vector.",
                "title": "Kind",
                "type": "string"
              },
              "name": {
                "description": "What the report and the manifest key on; unique among the run's terms.",
                "pattern": "^[A-Za-z_][A-Za-z0-9_]*$",
                "title": "Name",
                "type": "string"
              },
              "vector": {
                "$ref": "#/$defs/Vector",
                "default": "w",
                "description": "The decision vector the column multiplies."
              },
              "weight": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "pattern": "^(?!^[-+.]*$)[+-]?0*\\d*\\.?\\d*$",
                    "type": "string"
                  }
                ],
                "default": "1",
                "description": "Multiplies the term; negative for a reward, since the sum is minimized. A string, so the manifest records it exactly.",
                "title": "Weight"
              }
            },
            "required": [
              "name"
            ],
            "title": "linear term",
            "type": "object"
          }
        ],
        "description": "One objective term: an object whose `kind` names its model."
      },
      "title": "Objective",
      "type": "array"
    },
    "order_flow": {
      "$ref": "#/$defs/OrderFlow",
      "description": "The run's order flow: `inflow` — cash comes into the book, so the run buys: one variable per name, `w >= w0`, no sell vector, portfolios coupling through buys; `outflow`, the mirror: cash goes out, the run sells, coupling through sells; or `rebalance`: no cash moves on purpose, `w` may go either way inside its bounds with `buy` and `sell` the positive and negative parts of the change, portfolios coupling through every trade. A term or row that reads a side an inflow or an outflow lacks is refused, and so is a term that rewards `buy`, `sell`, or `trade` under a rebalance, where they are convex rather than affine. A desk's order flows are separate runs over one snapshot."
    },
    "post_solve": {
      "$ref": "#/$defs/PostSolveConfig",
      "description": "Verification tolerances."
    },
    "rules": {
      "default": [],
      "description": "Business-logic rule steps from `rules.py`, applied in order to each portfolio's bundle. Rules never see other portfolios.",
      "items": {
        "$ref": "#/$defs/RuleStep"
      },
      "title": "Rules",
      "type": "array"
    },
    "run": {
      "$ref": "#/$defs/RunMeta",
      "description": "Run identity: a name and tags for people, recorded in the manifest and kept out of the config hash."
    },
    "sink": {
      "$ref": "#/$defs/SinkStep",
      "description": "Sink step from `sinks.py`, called once with every solved portfolio's orders."
    },
    "solve": {
      "$ref": "#/$defs/SolveStep",
      "description": "The solve step from `solvers.py`: `(request: SolveRequest[, params]) -> SolveResult`. `cvxpy` (default) renders the terms and the typed constraint rows and solves with the solver its params name (`{\"name\": \"cvxpy\", \"params\": {\"solver\": \"CLARABEL\", \"time_limit_s\": 60}}`); a qualified name plugs in a firm's own library or a pure function for one side."
    },
    "solve_order": {
      "anyOf": [
        {
          "$ref": "#/$defs/SolveOrderStep"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Optional solve-order step from `solve_order.py`: `(data: PortfolioData[, params]) -> Decimal`, evaluated on each portfolio's ruled bundle. Lower keys solve first; ties break on `portfolio_id`. Replaces the portfolios frame's `solve_order` column."
    }
  },
  "required": [
    "run",
    "datasets",
    "order_flow",
    "sink"
  ],
  "title": "Portfolio optimizer run config",
  "type": "object"
}
